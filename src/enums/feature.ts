export enum FeatureCodeEnum {
  MANAGER = "manager",
  MEMBER = "member",
  FINANCIAL = "financial",
}

export enum FeatureCodeAction {
  READ = "read",
  DELETE = "delete",
  UPDATE = "update",
  CREATE = "create",
}
